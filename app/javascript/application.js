// Entry point for the build script in your package.json
console.log('Map:', Map);  // Verificar que Map existe
console.log('Map.prototype.get:', Map.prototype.get); // Verificar que get existe

// Entry point for the build script in your package.json
import "@hotwired/turbo-rails"
import "./controllers"
import * as bootstrap from "bootstrap"
