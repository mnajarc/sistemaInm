<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <!-- MARCA VISIBLE PARA DEBUG -->
    <span class="navbar-brand">
      🔹 NAVBAR DINÁMICO v2.0 🔹
    </span>
    
    <!-- Brand -->
    <%= link_to root_path, class: "navbar-brand d-flex align-items-center" do %>
      <i class="bi bi-building me-2"></i>
      <span>Sistema Inmobiliario</span>
    <% end %>

    <!-- DEBUG: Mostrar info del usuario -->
    <% if user_signed_in? %>
      <span class="navbar-text text-warning">
        DEBUG: <%= current_user.role.name %> - <%= current_user.email %>
      </span>
    <% end %>

    <!-- Mobile toggle button -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar content -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <% if user_signed_in? %>
        <!-- DEBUG: Contar menús -->
        <% menu_count = MenuItem.main_navigation(current_user).count %>
        <span class="navbar-text text-warning me-3">
          MENÚS: <%= menu_count %>
        </span>

        <!-- MENÚS DINÁMICOS -->
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <% MenuItem.main_navigation(current_user).each do |menu_item| %>
            <% children = menu_item.accessible_children_for_user(current_user) %>
            
            <% if children.any? %>
              <!-- Dropdown menu -->
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle text-white" href="#" role="button" 
                   data-bs-toggle="dropdown" aria-expanded="false">
                  🔸 <%= menu_item.display_name %> (<%= children.count %>)
                </a>
                <ul class="dropdown-menu">
                  <% children.each do |child| %>
                    <li>
                      <%= link_to (child.path.present? ? child.path : "#"), class: "dropdown-item" do %>
                        🔹 <%= child.display_name %>
                      <% end %>
                    </li>
                  <% end %>
                </ul>
              </li>
            <% else %>
              <!-- Simple menu -->
              <li class="nav-item">
                <%= link_to (menu_item.path.present? ? menu_item.path : "#"), class: "nav-link text-white" do %>
                  🔹 <%= menu_item.display_name %>
                <% end %>
              </li>
            <% end %>
          <% end %>
        </ul>

        <!-- User dropdown -->
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <%= button_to destroy_user_session_path, 
                method: :delete,
                class: "btn btn-outline-light btn-sm",
                data: { confirm: "¿Cerrar sesión?" } do %>
              Salir
            <% end %>
          </li>
        </ul>
      <% else %>
        <!-- Guest menu -->
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <%= link_to "Iniciar Sesión", new_user_session_path, class: "nav-link" %>
          </li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
