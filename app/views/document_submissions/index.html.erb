<!-- app/views/document_submissions/index.html.erb -->
<div class="container-fluid py-4" data-business-transaction-id="<%= @business_transaction.id %>">
<body data-business-transaction-id="<%= @business_transaction.id %>">

  <script>
    // 🔴 DESACTIVA TURBO globalmente en modales
    document.addEventListener('show.bs.modal', function() {
      Turbo.session.drive = false;
      console.log('Modal abierto - Turbo DESACTIVADO');
    });

    document.addEventListener('hidden.bs.modal', function() {
      // No reactive Turbo - mantén it off
      console.log('Modal cerrado - Turbo SIGUE DESACTIVADO');
    });

    // También bloquea Turbo Frame updates
    document.addEventListener('turbo:load', function() {
      Turbo.session.drive = false;
    });

    document.addEventListener('turbo:frame-load', function() {
      Turbo.session.drive = false;
    });
  </script>

  <div class="row mb-4">
    <div class="col-md-12">
      <div class="d-flex justify-content-between align-items-center">
        <h2>
          <i class="bi bi-file-earmark-check"></i>
          Checklist Documental
        </h2>
        <%= link_to business_transaction_path(@business_transaction), class: 'btn btn-outline-secondary' do %>
          <i class="bi bi-arrow-left"></i> Volver a Transacción
        <% end %>
      </div>
      
      <div class="text-muted mt-2">
        <strong>Transacción:</strong> <%= @business_transaction.id %> |
        <strong>Escenario:</strong> <%= @business_transaction.transaction_scenario&.name || 'Sin escenario' %> |
        <strong>Propiedad:</strong> <%= @business_transaction.property.address %>
      </div>
    </div>
  </div>

  <!-- Resumen de progreso -->
  <%= render 'shared/document_progress_summary', checklist: @checklist %>


  <!-- Botones de exportación -->
  <div class="mb-3 d-flex gap-2">
    <button id="copy-whatsapp-btn" class="btn btn-success">
      <i class="bi bi-whatsapp"></i> Copiar para WhatsApp
    </button>
    
    <a href="<%= export_checklist_business_transaction_document_submissions_path(@business_transaction, format: :pdf) %>" 
       class="btn btn-primary" 
       target="_blank">
      <i class="bi bi-download"></i> Descargar PDF
    </a>

    <a href="<%= export_checklist_business_transaction_document_submissions_path(@business_transaction, format: :text) %>" 
       class="btn btn-info"
       download="checklist_<%= @business_transaction.id %>.txt">
      <i class="bi bi-file-text"></i> Descargar TXT
    </a>
  </div>

  <script>
    document.getElementById('copy-whatsapp-btn').addEventListener('click', function() {
      const transactionId = <%= @business_transaction.id %>;
      const checklist = <%= @checklist.to_json.html_safe %>;
      
      let text = '📋 CHECKLIST DOCUMENTAL\n';
      text += '═'.repeat(60) + '\n\n';
      text += `Transacción: #${transactionId}\n`;
      text += `Propiedad: <%= @business_transaction.property.address %>\n`;
      text += `Escenario: <%= @business_transaction.transaction_scenario&.name || 'Sin escenario' %>\n`;
      text += `Fecha: <%= I18n.l(Date.today, format: :long) %>\n\n`;
      
      text += '📊 RESUMEN\n';
      text += '─'.repeat(60) + '\n';
      text += `Total de documentos: ${checklist.summary.total}\n`;
      text += `Cargados: ${checklist.summary.uploaded}\n`;
      text += `Pendientes: ${checklist.summary.pending}\n`;
      text += `Validados: ${checklist.summary.validated}\n`;
      text += `Rechazados: ${checklist.summary.rejected}\n`;
      text += `Progreso: ${checklist.summary.progress}%\n\n`;
      
      checklist.copropietarios.forEach(coData => {
        text += `👤 ${coData.co_owner.name}\n`;
        text += `   Rol: ${coData.co_owner.role}\n`;
        text += `   Porcentaje: ${coData.co_owner.percentage}%\n`;
        text += `   Documentos: ${coData.documents.total} | Cargados: ${coData.documents.uploaded} | Pendientes: ${coData.documents.total - coData.documents.uploaded}\n`;
        text += `   ${'─'.repeat(50)}\n\n`;
        
        coData.documents.list.forEach(category => {
          text += `   📁 ${category.category_display}\n`;
          category.documents.forEach(doc => {
            const status = doc.uploaded ? '✅' : '☐';
            text += `       ${status} ${doc.document_type.name}\n`;
          });
          text += '\n';
        });
      });
      
      navigator.clipboard.writeText(text).then(() => {
        alert('✅ Copiado al portapapeles. Ahora puedes pegarlo en WhatsApp');
      }).catch(() => {
        alert('❌ Error al copiar. Intenta de nuevo.');
      });
    });
  </script>




  <!-- Tabs para navegación entre copropietarios -->
  <div class="card mt-4">
    <div class="card-header bg-light">
      <h5 class="mb-0">
        <i class="bi bi-list-check"></i>
        Documentos por Copropietario
      </h5>
    </div>
    
    <% if @checklist[:copropietarios].present? && @checklist[:copropietarios].any? %>
      <!-- Navegación con Tabs -->
      <ul class="nav nav-tabs" role="tablist" style="padding: 0.75rem 1rem 0 1rem; background: #f8f9fa;">
        <% @checklist[:copropietarios].each_with_index do |co_data, idx| %>
          <li class="nav-item" role="presentation">
            <button class="nav-link <%= 'active' if idx == 0 %>" 
                    id="co-owner-<%= idx %>-tab" 
                    data-bs-toggle="tab" 
                    data-bs-target="#co-owner-<%= idx %>-content" 
                    role="tab"
                    aria-controls="co-owner-<%= idx %>-content"
                    aria-selected="<%= idx == 0 %>">
              <i class="bi bi-person-vcard"></i>
              <%= co_data[:co_owner][:name] %>
              <span class="badge bg-secondary ms-2"><%= co_data[:documents][:total] %></span>
            </button>
          </li>
        <% end %>
      </ul>

      <!-- Contenido de Tabs -->
      <div class="tab-content">
        <% @checklist[:copropietarios].each_with_index do |co_data, idx| %>
          <div class="tab-pane fade <%= 'show active' if idx == 0 %>" 
              id="co-owner-<%= idx %>-content" 
              role="tabpanel"
              aria-labelledby="co-owner-<%= idx %>-tab">
            
            <div class="p-4">
              <!-- Información del copropietario -->
              <div class="mb-3 p-3 bg-light border-start border-4 border-primary">
                <div class="row">
                  <div class="col-md-6">
                    <small class="text-muted">
                      <strong><%= co_data[:co_owner][:percentage] %>%</strong> - 
                      <%= co_data[:co_owner][:role].titleize %>
                    </small>
                  </div>
                  <div class="col-md-6 text-end">
                    <% unless co_data[:co_owner][:active] %>
                      <span class="badge bg-warning text-dark">Pendiente</span>
                    <% end %>
                  </div>
                </div>
              </div>

              <!-- Tabla de documentos -->

              <div class="table-responsive">
                <table class="table table-hover">
                  <thead>
                    <tr>
                      <th>Documento</th>
                      <th>Estado</th>
                      <th>Vigencia</th>
                      <th>Subido por</th>
                      <th>Fecha</th>
                      <th>Acciones</th>
                    </tr>
                  </thead>
                
          <tbody>
            <% co_data[:documents][:list].each do |category| %>    <!-- ← SIN el loop de copropietarios -->
              <% category[:documents].each do |doc| %>
                <tr>
                  <td>
                    <strong><%= doc[:document_type][:name] %></strong>
                    <br>
                    <small class="text-muted"><%= category[:category_display] %></small>
                  </td>
                  <td>
                    <% submission = @business_transaction.document_submissions
                                    .find_by(document_type_id: doc[:document_type][:id],
                                            business_transaction_co_owner_id: co_data[:co_owner][:id]) %>
                    <% if submission.present? %>
                      <span class="badge <%= submission.document_status&.name == 'validado' ? 'bg-success' : submission.document_status&.name == 'rechazado' ? 'bg-danger' : 'bg-warning' %>">
                        <%= submission.document_status&.name&.titleize %>
                      </span>
                    <% else %>
                      <span class="badge bg-secondary">Sin subir</span>
                    <% end %>
                  </td>
                  <td>
                    <small><%= doc[:vigencia] || 'Sin vigencia' %></small>
                  </td>
                  <td>
                    <small><%= submission&.uploaded_by&.email || '—' %></small>
                  </td>
                  <td>
                    <small><%= submission&.submitted_at&.strftime('%d/%m/%y') || '—' %></small>
                  </td>
                  <td>
                    <%= render 'document_row', document: doc, co_owner: co_data[:co_owner], submission: submission %>
                  </td>
                </tr>
              <% end %>
            <% end %>
          </tbody>



                </table>
              </div>


            </div>
          </div>
        <% end %>
      </div>

    <% else %>
      <div class="card-body">
        <div class="alert alert-info">
          <i class="bi bi-info-circle"></i> Sin copropietarios configurados
        </div>
      </div>
    <% end %>
  </div>



  <!-- Adquiriente (si existe) -->
  <% if @business_transaction.acquiring_client.present? && @checklist[:adquiriente][:total].positive? %>
    <div class="card mt-4">
      <div class="card-header bg-light">
        <h5 class="mb-0">
          <i class="bi bi-person-check"></i>
          Adquiriente
        </h5>
      </div>
      <div class="card-body">
        <p class="text-muted">
          <strong><%= @business_transaction.acquiring_client.display_name %></strong>
        </p>
        
        <div class="table-responsive">
          <table class="table table-hover table-sm">
            <thead class="table-light">
              <tr>
                <th style="width: 35%;">Documento</th>
                <th style="width: 12%;">Estado</th>
                <th style="width: 12%;">Vigencia</th>
                <th style="width: 10%;">Subido por</th>
                <th style="width: 8%;">Fecha</th>
                <th style="width: 23%;">Acciones</th>
              </tr>
            </thead>
            <tbody>
              <!-- Similar estructura a copropietarios -->
              <!-- ... (repetir tabla para adquiriente) ...-->
            </tbody>
          </table>
        </div>
      </div>
    </div>
  <% end %>
</div>

<!-- Modal global para RECHAZAR -->
<div class="modal fade" 
     id="rejectModal" 
     tabindex="-1"
     data-turbo="false">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header bg-danger text-white">
        <h5 class="modal-title">Rechazar Documento</h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
      </div>
      <form id="rejectForm" 
            method="POST" 
            data-turbo="false"
            onsubmit="return confirm('¿Rechazar este documento?')">
        <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
        <div class="modal-body">
          <div class="mb-3">
            <label for="rejection_reason" class="form-label">Motivo del rechazo *</label>
            <textarea name="rejection_reason" 
                     id="rejection_reason"
                     class="form-control"
                     rows="4"
                     placeholder="Explica por qué rechazas este documento..."
                     required></textarea>
          </div>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
          <button type="submit" class="btn btn-danger">Rechazar</button>
        </div>
      </form>
    </div>
  </div>
</div>



<style>
  .btn-group-sm .btn {
    padding: 0.25rem 0.5rem;
    font-size: 0.85rem;
  }
  
  .table-hover tbody tr:hover {
    background-color: rgba(0, 0, 0, 0.02);
  }
</style>
